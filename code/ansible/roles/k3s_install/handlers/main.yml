---
- name: restart k3s
  ansible.builtin.systemd:
    name: k3s
    state: restarted
  when: k3s_role == "server"

- name: restart k3s-agent
  ansible.builtin.systemd:
    name: k3s-agent
    state: restarted
  when: k3s_role == "agent"
