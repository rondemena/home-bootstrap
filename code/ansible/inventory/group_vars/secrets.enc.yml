# WARNING: This file MUST be encrypted with SOPS before committing!
# Run: sops --encrypt --in-place code/ansible/inventory/group_vars/secrets.enc.yml
#
# This file is a TEMPLATE. Replace placeholder values with actual secrets.
# After editing, encrypt with: sops code/ansible/inventory/group_vars/secrets.enc.yml

# iLO credentials (shared across all iLO targets)
ilo_pass: "REPLACE_WITH_ILO_PASSWORD"

# Proxmox API token
pve_api_token_id: "automation@pam!tofu"
pve_api_token_secret: "REPLACE_WITH_PVE_API_TOKEN"

# k3s cluster join token (generated during server install, populated later)
k3s_token: "REPLACE_AFTER_K3S_SERVER_INSTALL"
